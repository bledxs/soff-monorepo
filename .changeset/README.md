# Changesets

Aquí es donde se guardan los archivos de changesets.

Para crear un nuevo changeset, ejecuta:

```bash
npm run changeset
```

Para más información, visita [la documentación de Changesets](https://github.com/changesets/changesets).
